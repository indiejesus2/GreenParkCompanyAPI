<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <%= image_tag(attachments['logo.png'].url) %>
    <div class="message">
    <p>
      Awesome job <%= @employee.profile.name %>!
      Your application has been successfully delivered to <b> <%= @job.company %> </b>. Look forward to them contacting you soon about the <b> <%= @job.title %> </b> position . 
    </p>
    <p>
      <% if @jobs.length > 1 %>
      Make sure to check out these other jobs that you matched with.
      <% elsif @jobs.length == 1 %>
      Make sure to check out this other job that you matched with.
      <% else %>
      For the meantime, BluCollar will continue looking for jobs that match your profile.
      <% end %>
    </p> 
    </div>
    <% if @jobs.length > 0 %>
    <div class="jobs">
        <% @jobs.each do |job| %>
      <div class="job">
          <span class="jobTitle">
            <h2><%= @job.title %></h2>
            <span class="jobDetails">
              <h3><%= @job.employer.name %> | <%= @job.address %></h3>
            </span>
        </span>
      </div>
        <% end %>
    </div>
    <% end %>
    <span class="below">
        To login to the site, just follow this link: <a href=<%= @url %> target="_blank">BluCollar</a>.
    </span>
    <p>Happy job-hunting and have a great day!</p>
    <p>BluCollar</p>
  </body>
</html>