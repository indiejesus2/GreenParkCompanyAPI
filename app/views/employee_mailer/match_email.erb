<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <%= image_tag(attachments['logo.png'].url) %>
    <div class="message">
    <span class="above">
        Incredible news <%= @employee.profile.name %>!
        We have found a job that matches your profile.
    </span>
      <div class="job">
        <span class="jobTitle">
            <h2><%= @job.title %></h2>
            <span class="jobDetails">
              <h3><%= @job.employer.name %> | <%= @job.address %></h3>
            </span>
        </span>
      </div>
    <span class="below">
        To login to the site, just follow this link: <a href=<%= @url %> target="_blank">BluCollar</a>.
    </span>
    <p>Happy job-hunting and have a great day!</p>
    <p>BluCollar</p>
  </body>
</html>